@model IEnumerable<FotoDB.Models.KrajModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.KrajModelID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nazwa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.KrajModelID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nazwa)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.KrajModelID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.KrajModelID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.KrajModelID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
